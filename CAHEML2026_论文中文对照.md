# 基于迁移学习的混凝土表面裂缝检测方法对比研究（中文对照版）

**会议：** CAHEML 2026 — 土木建筑、水利工程与机器学习国际会议
**投稿截止：** 2026 年 8 月 13 日

---

## 摘要

混凝土裂缝检测是桥梁巡检和结构健康监测中的关键任务。传统人工目视检测效率低、主观性强，对宽度小于 0.2 毫米的细微裂缝难以识别。本文基于 SDNET2018 数据集（含有 56,092 张真实混凝土桥面、路面和墙体图像），对比了三种机器学习方法在混凝土表面裂缝检测中的表现：（1）使用原始像素特征的随机森林基线模型；（2）自建的四层卷积神经网络（5.3M 参数）；（3）基于 ImageNet 预训练权重的 ResNet18 迁移学习方法。实验结果表明，随机森林仅达到 59.10% 的准确率，说明像素级特征不足以捕获裂缝的纹理信息；自建 CNN 将准确率提升至 75.87%；ResNet18 迁移学习方法表现最优，准确率达到 83.58%，裂缝召回率约 83%，无裂缝精确率约 89%。训练后的模型大小约 45MB，可在移动端离线部署，适用于现场桥梁检测场景。本研究表明，迁移学习相比传统机器学习方法和自建 CNN 能显著提升混凝土裂缝检测精度，且轻量化模型具备实际部署可行性。

**关键词：** 混凝土裂缝检测；迁移学习；ResNet18；卷积神经网络；结构健康监测；SDNET2018

---

## 1. 引言

中国现存公路桥梁超过 87.5 万座，其中大量桥梁服役已超过三十年[1]。根据现行规范，每座桥梁需每 2 至 5 年进行一次定期检测[2]。传统检测方法依赖专业人员使用裂缝测宽仪逐点测量，一座中等跨度的桥梁通常需要 3 至 5 名检测人员工作 7 至 10 天，其中搭设脚手架和安全设备的时间占整个工期的相当比例[3]。

人工目视检测存在三个根本问题。第一，效率低下——检测人员每天可覆盖的面积有限，桥墩高处和水下结构需要昂贵的辅助设备。第二，主观性强——不同人员对同一裂缝的判断可能不一致，且在连续工作两小时后检测一致性显著下降[4]。第三，宽度小于 0.2 毫米的细微裂缝在现场条件下几乎无法被人眼识别[5]。

近年来，深度学习方法在自动化裂缝检测中展现了显著优势。卷积神经网络能从原始图像数据中学习层次化特征，无需人工设计特征提取器[6]。迁移学习——即将在大规模图像数据集上预训练的模型微调至特定任务——进一步提升了在有限训练数据场景下的检测性能[7]。

本文对三种机器学习方法进行了系统性对比研究：传统随机森林基线、从零训练的自建 CNN、以及基于迁移学习的 ResNet18。三种方法在相同的 SDNET2018 数据集和一致的实验条件下训练与评估，以公平比较各自的优势和局限。

本文的主要贡献为：（1）在大规模真实混凝土裂缝数据集上进行了三种 ML 方法的直接对比；（2）证明了迁移学习相较传统 ML 基线可将裂缝检测准确率提升二十余个百分点；（3）分析了模型部署可行性，训练后的模型可在移动设备上离线运行，体积不足 50MB。

---

## 2. 相关工作

### 2.1 传统图像处理方法

早期自动化裂缝检测主要依赖图像处理技术，如边缘检测（Canny、Sobel）、阈值分割（Otsu 法）和形态学操作[8]。这些方法在受控光照条件下可以识别明显裂缝，但对真实检测场景中的光照变化、表面纹理差异和图像噪声高度敏感[9]。

### 2.2 基于 CNN 的裂缝检测

深度学习方法在裂缝检测中的应用始于 LeNet、AlexNet、VGG 等架构对裂缝/非裂缝图像块的二分类[10]。研究者随后探索了更深层的架构，包括用于像素级裂缝分割的 U-Net 编码器-解码器网络[11]，以及用于裂缝定位的 YOLO、Faster R-CNN 等目标检测框架[12]。

### 2.3 迁移学习在土木工程中的应用

迁移学习已成功应用于多种土木工程任务。ImageNet 预训练模型已被微调用于混凝土损伤分类[13]、钢结构腐蚀检测[14]和路面破损识别[15]。其核心优势在于，从数百万张自然图像中学习到的底层特征（边缘、纹理、形状）可有效迁移至工程材料表面，从而减少任务特定训练数据的需求量。

### 2.4 SDNET2018 数据集

SDNET2018 是一个公开发布的混凝土裂缝检测基准数据集，由美国犹他州立大学的 Maguire 等人发布[16]。数据集包含 56,092 张混凝土表面图像（桥面、路面、墙体），裂缝宽度范围为 0.06 毫米至 25 毫米。每张图像被标注为有裂缝或无裂缝。数据集覆盖三种常见的混凝土结构类型：D（桥面，8,484 张裂缝图 + 47,608 张无裂缝图）、P（路面）和 W（墙体）。选择该数据集的原因在于其代表了真实检测条件下的多样化光照、表面纹理和裂缝形态。

---

## 3. 方法

### 3.1 数据集与预处理

所有实验均使用 SDNET2018 数据集。从总计 56,092 张图像中抽取平衡子集，确保裂缝类与无裂缝类的等量样本。每次实验使用分层抽样按 85% 训练集、15% 测试集划分数据。

图像根据模型架构要求统一缩放（随机森林为 64×64，CNN 为 96×96 或 128×128，ResNet18 为 160×160）。像素值归一化至 [0, 1] 区间。对于 CNN 和 ResNet18，应用 ImageNet 逐通道标准化（mean=[0.485, 0.456, 0.406]，std=[0.229, 0.224, 0.225]）。数据增强使用训练时的随机水平翻转。

### 3.2 随机森林基线

作为基线方法，在展平的灰度像素值上训练随机森林分类器。每张 64×64 图像转为灰度图后展平为 4,096 维特征向量。使用 scikit-learn 训练包含 200 棵树、最大深度为 15 的随机森林。共使用 5,000 张图像（裂缝 2,500 + 无裂缝 2,500），按 80:20 划分。

### 3.3 自建 CNN

从零设计了四层卷积神经网络：

- 第 1 块：Conv(3→32, 3×3) → BatchNorm → ReLU → Conv(32→32) → BatchNorm → ReLU → MaxPool(2×2) → Dropout(0.25)
- 第 2 块：Conv(32→64, 3×3) → BatchNorm → ReLU → Conv(64→64) → BatchNorm → ReLU → MaxPool(2×2) → Dropout(0.25)
- 第 3 块：Conv(64→128, 3×3) → BatchNorm → ReLU → Conv(128→128) → BatchNorm → ReLU → MaxPool(2×2) → Dropout(0.2)
- 第 4 块：Conv(128→256, 3×3) → BatchNorm → ReLU → MaxPool(2×2) → Dropout(0.25)
- 分类器：Flatten → Dense(256×(S/16)², 512) → ReLU → Dropout(0.5) → Dense(512, 2)

模型参数量约 530 万。输入尺寸为 96×96×3。使用 AdamW 优化器（lr=0.001, weight_decay=1e-4）和交叉熵损失训练 15 轮。共使用 10,000 张图像（每类 5,000 张）进行训练和验证。

### 3.4 ResNet18 迁移学习

使用 ImageNet1K 预训练的 ResNet18 作为基座模型。将原始最后的全连接层替换为自定义分类器：Dropout(0.5) → Linear(512, 2)。整个网络端到端微调。输入尺寸为 160×160×3。使用 AdamW 优化器（lr=0.0003, weight_decay=1e-4）和交叉熵损失训练 12 轮。共使用 8,000 张图像（每类 4,000 张）。

所有实验在 MacBook Pro 上使用 Apple MPS GPU 加速完成，证明该训练流程不需要云端 GPU 资源。

---

## 4. 实验

略（与英文版一致——评估指标、训练参数对比表）

---

## 5. 结果与讨论

### 5.1 分类准确率

| 方法 | 准确率 | 裂缝召回率 | 无裂缝准确率 |
|------|--------|-----------|------------|
| 随机森林 | 59.10% | — | — |
| 自建 CNN | 75.87% | — | — |
| **ResNet18 迁移学习** | **83.58%** | **~83%** | **~89%** |

### 5.2 混淆矩阵分析

ResNet18 模型的裂缝召回率约为 83%，即 100 条真实裂缝中有 83 条被正确识别，17 条被遗漏。无裂缝精确率约为 89%，说明 11% 的无裂缝表面被误判为裂缝。

### 5.3 为什么迁移学习最优

随机森林基线（59.10%）表明，仅靠像素值不足以识别裂缝——图像被展平为一维向量时，区分裂缝与表面纹理所需的空间结构信息丢失了。

自建 CNN（75.87%）比基线高出约 17 个百分点，证实了卷积滤波器能够学到裂缝的线状特征。然而在较小的数据集（8,500 张图像）上从零训练，限制了模型的泛化能力。

ResNet18 迁移学习（83.58%）再提升约 8 个百分点。ImageNet 预训练权重提供了可有效迁移至混凝土表面的底层特征提取器（边缘检测、纹理分析）。裂缝特定的微调将这些通用特征适配到辨别裂缝与模板接缝线、阴影、污渍等表面不规则性的狭义任务上。

### 5.4 误差分析

约 17% 的漏检裂缝（假阴性）主要发生在宽度极细（<0.1mm）或光照差的图像中，裂缝表现为模糊阴影而非清晰线条。约 11% 的假阳性主要由模板接缝线、深色表面污渍和形似裂缝的阴影造成。

### 5.5 部署可行性

训练后的 ResNet18 模型约 45MB，推理不需要云端连接。在 MacBook Pro MPS GPU 上单张图像推理耗时小于 0.1 秒。这使得模型适合部署在移动设备或边缘计算单元上，适用于没有网络连接的桥梁检测现场。

---

## 6. 结论

本文使用 SDNET2018 数据集对三种机器学习方法在自动化混凝土表面裂缝检测中的表现进行了对比研究。实验结果表明：

1. 使用像素特征的传统机器学习（随机森林，59.10%）因丢失空间纹理信息而不足以完成裂缝检测任务。
2. 自建 CNN（75.87%）通过直接从图像中学习层次化特征，显著提升了检测性能。
3. 基于 ResNet18 的迁移学习（83.58%）利用预训练视觉特征并在裂缝特定数据上微调，取得了最优结果。

轻量化模型（45MB，推理 <0.1s）适合在移动设备上离线部署，可用于实际的现场检测。未来工作将聚焦于：基于 U-Net 架构的像素级裂缝分割、裂缝宽度的定量化测量，以及与 BIM 模型的集成以实现裂缝全生命周期追踪。

---

## 参考文献（16 篇，与英文版一致）
